{
  "name": "gsap-lesson",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "npm run server & npm run sass",
    "server": "live-server",
    "sass": "sass styles --watch"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "prettier": "^2.2.1",
    "sass": "^1.32.8"
  }
}
